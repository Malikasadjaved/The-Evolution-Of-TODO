# ============================================================================
# Frontend Chatbot Environment Variables - Phase 3
# ============================================================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.local: cp .env.local.example .env.local
# 2. Update the values below for your environment
# 3. NEVER commit .env.local to version control (already in .gitignore)
#
# Constitution: .specify/memory/phase-3-constitution.md (v1.1.0)
# Spec: specs/002-ai-chatbot-mcp/spec.md Section 8.5 (Environment Config)
# ============================================================================

# ============================================================================
# REQUIRED: Backend API Configuration
# ============================================================================

# Backend API Base URL
# - Development: http://localhost:8000 (FastAPI backend running locally)
# - Production: Your deployed backend URL (e.g., https://api.yourapp.com)
#
# IMPORTANT: This MUST match the backend server's actual URL
# Next.js will proxy /api/* requests to this URL (see next.config.js)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# OPTIONAL: Authentication Configuration
# ============================================================================

# Phase 2 Web UI URL (for login/logout redirects)
# - Development: http://localhost:3000 (Next.js web UI)
# - Production: Your deployed web UI URL
#
# Used for:
# - Redirecting to login page when not authenticated
# - "Open Web UI" link in chatbot header
# - Logout redirect
NEXT_PUBLIC_WEB_UI_URL=http://localhost:3000

# ============================================================================
# OPTIONAL: Feature Flags
# ============================================================================

# Enable debug logging in browser console
# - true: Show detailed API requests/responses in console
# - false: Only show errors (production mode)
NEXT_PUBLIC_DEBUG_MODE=false

# Enable development features (mock user ID, etc.)
# - true: Allow mock authentication for testing
# - false: Require real JWT tokens (production mode)
NEXT_PUBLIC_DEV_MODE=true

# ============================================================================
# OPTIONAL: OpenAI Client-Side Features (Future Use)
# ============================================================================

# OpenAI API Key (for client-side features)
# Note: Phase 3 backend handles all OpenAI API calls server-side
# Only set this if implementing client-side OpenAI features (e.g., voice input)
#
# WARNING: NEVER commit real API keys to version control!
# NEXT_PUBLIC_OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================================================
# OPTIONAL: Analytics & Monitoring (Future Use)
# ============================================================================

# Google Analytics Measurement ID
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Sentry DSN for error tracking
# NEXT_PUBLIC_SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ============================================================================
# NOTES
# ============================================================================
#
# Environment Variable Naming Convention:
# - NEXT_PUBLIC_* variables are exposed to the browser (client-side)
# - Variables without NEXT_PUBLIC_ prefix are server-side only
# - Use NEXT_PUBLIC_ prefix ONLY for non-sensitive values
#
# For sensitive values (API keys, secrets):
# - NEVER use NEXT_PUBLIC_ prefix (would expose to browser)
# - Store in backend environment variables instead
# - Backend handles all sensitive API calls
#
# ============================================================================
