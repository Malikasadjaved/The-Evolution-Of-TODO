/**
 * Type Exports - Phase 3 AI Chatbot Frontend
 *
 * Central export point for all TypeScript types.
 * Import types from here for consistency across the application.
 *
 * Usage:
 * ```typescript
 * import type { ChatRequest, ChatResponse, Message } from '@/types';
 * ```
 */

// Export all API types
export * from "./api";

// Export CSS module types (already defined in css.d.ts)
// No need to re-export, TypeScript will pick it up automatically
