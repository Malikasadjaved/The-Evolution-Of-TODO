# =============================================================================
# Kubernetes Deployment Secrets
# =============================================================================
# IMPORTANT: Copy this file to .env.secrets and fill in your actual values
# DO NOT commit .env.secrets to Git (it's in .gitignore)
# =============================================================================

# PostgreSQL Database URL (Neon or any PostgreSQL provider)
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://user:password@host.neon.tech/todo_db?sslmode=require"

# Better Auth Secret (must be at least 32 characters, recommend 43)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# This MUST match between backend and frontend
BETTER_AUTH_SECRET="your-43-character-secret-here-generate-with-command-above"

# OpenAI API Key (for AI chatbot features)
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-proj-your-openai-api-key-here"
